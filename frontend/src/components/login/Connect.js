import React from "react";
import { Link } from "react-router-dom"

const Connect = () => {
    return (
        <div>
            <div className=''>
            <Link className='' to="/signin">Connect to start</Link>
            </div>
        </div>
    )
}

export default Connect